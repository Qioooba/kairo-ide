name: Kairo IDE CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GO_VERSION: '1.22'
  NODE_VERSION: '20'
  FIXED_TOMCAT_VERSION: '6.0.53'
  # TODO: replace with the real sha256 of apache-tomcat-6.0.53.tar.gz
  FIXED_TOMCAT_CHECKSUM: 'sha256:PLACEHOLDER_UPDATE_BEFORE_RELEASE'
  FIXED_JDTLS_VERSION: '1.43.0'
  # TODO: replace with the real sha256 of the downloaded jdtls archive
  FIXED_JDTLS_CHECKSUM: 'sha256:PLACEHOLDER_UPDATE_BEFORE_RELEASE'

jobs:
  # =========================================================================
  # 1. Go unit tests
  # =========================================================================
  go-unit-linux:
    name: Go Unit (Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: gofmt check
        working-directory: runtime-agent
        # gofmt -l always exits 0, so fail on any listed file.
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        working-directory: runtime-agent
        run: go vet ./...

      - name: Unit tests with race detector
        working-directory: runtime-agent
        run: go test -race -v -count=1 -timeout 420s ./...

      - name: Upload agent logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: go-unit-logs
          path: |
            /tmp/kairo-agent*.log
            /tmp/kairo-test*.log
          if-no-files-found: ignore

  # =========================================================================
  # 2. Go cross-build
  # =========================================================================
  go-cross-build:
    name: Go Cross-Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        os: [linux, windows, darwin]
        arch: [amd64]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Cross-build agent (${{ matrix.os }}/${{ matrix.arch }})
        working-directory: runtime-agent
        run: |
          GOOS=${{ matrix.os }} GOARCH=${{ matrix.arch }} CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o ../dist/kairo-agent-${{ matrix.os }}-${{ matrix.arch }} ./cmd/kairo-runtime

      - name: Upload agent binary
        uses: actions/upload-artifact@v4
        with:
          name: kairo-agent-${{ matrix.os }}-${{ matrix.arch }}
          path: dist/kairo-agent-${{ matrix.os }}-${{ matrix.arch }}

  # =========================================================================
  # 3. TypeScript clean build + lint + unit
  # =========================================================================
  ts-clean-build:
    name: TS Clean Build + Lint + Unit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Clean build outputs
        run: pnpm clean

      - name: Build all packages
        run: pnpm build

      - name: Type-check packages
        run: pnpm -r --filter "./packages/*" exec tsc --noEmit

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm -r --filter './packages/*' test

      - name: Encoding round-trip tests
        run: pnpm --filter @kairo/encoding-extension test

      - name: Runtime client tests
        run: pnpm --filter @kairo/runtime-extension exec node --test src/browser/runtime.test.cjs src/browser/runtime-dynamic-routes.test.cjs

      - name: Kairo commands tests
        run: pnpm --filter @kairo/theia-product exec node --test src/main/browser/kairo-commands.test.cjs

      - name: Upload test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ts-test-logs
          path: |
            packages/*/lib/**/*.log
            /tmp/kairo-*.log
          if-no-files-found: ignore

  # =========================================================================
  # 4. Contract tests
  # =========================================================================
  contract:
    name: Contract Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [ts-clean-build]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Contract endpoint tests
        run: node --test tests/contract/contract.test.cjs

      - name: EventStream contract tests
        run: node --test tests/contract/eventstream.test.cjs

      - name: Fault injection tests
        run: node --test tests/fault/fault-injection.test.cjs

      - name: Performance baseline
        run: node --test tests/perf/perf-baseline.test.cjs

      - name: Upload contract test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: contract-test-results
          path: testdata/contracts/
          if-no-files-found: ignore

  # =========================================================================
  # 5. Integration core
  # =========================================================================
  integration-core:
    name: Integration Core
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [go-unit-linux, ts-clean-build]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Build agent
        working-directory: runtime-agent
        run: go build -trimpath -o ../dist/kairo-runtime ./cmd/kairo-runtime

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Start agent
        run: |
          ./dist/kairo-runtime --bind 127.0.0.1 --port 18080 &
          echo "AGENT_PID=$!" >> $GITHUB_ENV
          sleep 2

      - name: Sample import test
        run: |
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/workspaces \
            -H 'Content-Type: application/json' \
            -d '{"rootPath":"/tmp/test-workspace","name":"Test"}'

      - name: Search test
        run: |
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/search \
            -H 'Content-Type: application/json' \
            -d '{"workspaceId":"test","query":"test","isRegex":false,"caseSensitive":false,"maxResults":10}'

      - name: Encoding test
        run: |
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/encoding/validate \
            -H 'Content-Type: application/json' \
            -d '{"text":"你好","encoding":"gbk"}'

      - name: Build test
        run: |
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/builds \
            -H 'Content-Type: application/json' \
            -d '{"projectId":"test","clean":true}'

      - name: Stop agent
        if: always()
        run: |
          if [ -n "$AGENT_PID" ]; then
            kill $AGENT_PID 2>/dev/null || true
          fi

      - name: Upload agent logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-core-agent-logs
          path: /tmp/kairo-agent*.log
          if-no-files-found: ignore

  # =========================================================================
  # 6. Integration Tomcat (Linux)
  # =========================================================================
  integration-tomcat-linux:
    name: Integration Tomcat (Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [go-unit-linux]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Download fixed Tomcat
        run: |
          TOMCAT_URL="https://archive.apache.org/dist/tomcat/tomcat-6/v${FIXED_TOMCAT_VERSION}/bin/apache-tomcat-${FIXED_TOMCAT_VERSION}.tar.gz"
          mkdir -p dist/tomcat
          curl -fsSL "$TOMCAT_URL" -o dist/tomcat/apache-tomcat.tar.gz
          echo "${FIXED_TOMCAT_CHECKSUM#sha256:}  dist/tomcat/apache-tomcat.tar.gz" > dist/tomcat/checksum
          sha256sum -c dist/tomcat/checksum
          tar -xzf dist/tomcat/apache-tomcat.tar.gz -C dist/tomcat/

      - name: Build agent
        working-directory: runtime-agent
        run: go build -trimpath -o ../dist/kairo-runtime ./cmd/kairo-runtime

      - name: Start agent with Tomcat
        run: |
          ./dist/kairo-runtime --bind 127.0.0.1 --port 18080 --tomcat-home dist/tomcat/apache-tomcat-${FIXED_TOMCAT_VERSION} &
          echo "AGENT_PID=$!" >> $GITHUB_ENV
          sleep 3

      - name: Health check
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:18080/api/v1/health | grep -q '"ok":true'; then
              echo "Agent healthy"
              exit 0
            fi
            sleep 1
          done
          echo "Agent not healthy within 30s" >&2
          exit 1

      - name: Full closed-loop test
        run: |
          set -euo pipefail
          # Import workspace
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/workspaces \
            -H 'Content-Type: application/json' \
            -d '{"rootPath":"/tmp/legacy-sample","name":"Legacy Sample"}'

          # Scan workspace
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/workspaces/ws-1/scan \
            -H 'Content-Type: application/json' \
            -d '{"deep":true}'

          # Build
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/builds \
            -H 'Content-Type: application/json' \
            -d '{"projectId":"proj-1","clean":true}'

          # Deploy
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/deployments \
            -H 'Content-Type: application/json' \
            -d '{"projectId":"proj-1","buildId":"build-1"}'

          # Start server
          curl -fsS -X POST http://127.0.0.1:18080/api/v1/servers \
            -H 'Content-Type: application/json' \
            -d '{"projectId":"proj-1"}'

          sleep 10

          # Check server
          curl -fsS http://127.0.0.1:18080/api/v1/servers

          # Stop server
          curl -fsS -X DELETE http://127.0.0.1:18080/api/v1/servers/srv-1 \
            -H 'Content-Type: application/json' \
            -d '{"force":false}'

      - name: Stop agent
        if: always()
        run: |
          if [ -n "$AGENT_PID" ]; then
            kill $AGENT_PID 2>/dev/null || true
          fi

      - name: Upload Tomcat logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tomcat-logs
          path: |
            dist/tomcat/apache-tomcat-*/logs/
            /tmp/kairo-agent*.log
          if-no-files-found: ignore

  # =========================================================================
  # 7. Theia Java E2E
  # =========================================================================
  theia-java-e2e:
    name: Theia Java E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [ts-clean-build]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Build agent
        working-directory: runtime-agent
        run: go build -trimpath -o ../dist/kairo-runtime ./cmd/kairo-runtime

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Download fixed JDT LS
        run: |
          mkdir -p dist/jdtls
          JDTLS_URL="https://download.eclipse.org/jdtls/milestones/${FIXED_JDTLS_VERSION}/jdt-language-server-${FIXED_JDTLS_VERSION}-20240125.tar.gz"
          curl -fsSL "$JDTLS_URL" -o dist/jdtls/jdtls.tar.gz
          echo "${FIXED_JDTLS_CHECKSUM#sha256:}  dist/jdtls/jdtls.tar.gz" > dist/jdtls/checksum
          sha256sum -c dist/jdtls/checksum
          tar -xzf dist/jdtls/jdtls.tar.gz -C dist/jdtls/

      - name: Start agent
        run: |
          ./dist/kairo-runtime --bind 127.0.0.1 --port 18080 --jdtls-home dist/jdtls &
          echo "AGENT_PID=$!" >> $GITHUB_ENV
          sleep 3

      - name: Start Theia
        env:
          # The frontend reads KAIRO_RUNTIME_URL to address the
          # Runtime Agent (see scripts/dev.sh); without it API
          # calls go to the Theia origin (port 3000) and 404.
          KAIRO_RUNTIME_URL: http://127.0.0.1:18080
        run: |
          cd packages/theia-product
          npx theia start --hostname=127.0.0.1 --port=3000 &
          echo "THEIA_PID=$!" >> $GITHUB_ENV
          sleep 10

      - name: Install Playwright
        run: pnpm exec playwright install chromium

      - name: Run Playwright E2E
        run: pnpm test:e2e:web

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-screenshots
          path: docs/screenshots/
          if-no-files-found: ignore

      - name: Upload Theia logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: theia-logs
          path: |
            /tmp/kairo-agent*.log
            packages/theia-product/logs/
          if-no-files-found: ignore

      - name: Stop processes
        if: always()
        run: |
          if [ -n "$THEIA_PID" ]; then kill $THEIA_PID 2>/dev/null || true; fi
          if [ -n "$AGENT_PID" ]; then kill $AGENT_PID 2>/dev/null || true; fi

  # =========================================================================
  # 8. Desktop smoke (Windows)
  # =========================================================================
  desktop-smoke-windows:
    name: Desktop Smoke (Windows)
    runs-on: windows-latest
    timeout-minutes: 20
    needs: [go-cross-build, ts-clean-build]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Build agent (Windows)
        working-directory: runtime-agent
        run: |
          go build -trimpath -o ../dist/kairo-runtime.exe ./cmd/kairo-runtime

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Start agent (Windows)
        run: |
          Start-Process -NoNewWindow -FilePath "dist/kairo-runtime.exe" -ArgumentList "--bind","127.0.0.1","--port","18080"
          Start-Sleep -Seconds 3

      - name: Smoke test (health check)
        run: |
          $response = Invoke-WebRequest -Uri "http://127.0.0.1:18080/api/v1/health" -UseBasicParsing
          Write-Host "Agent health: $($response.StatusCode)"
          if ($response.StatusCode -ne 200) { throw "Health check failed" }

      - name: Stop agent
        if: always()
        run: |
          Get-Process -Name "kairo-runtime" -ErrorAction SilentlyContinue | Stop-Process -Force

      - name: Upload agent logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-agent-logs
          path: ${{ env:TEMP }}/kairo-agent*.log
          if-no-files-found: ignore

  # =========================================================================
  # 9. Desktop smoke (macOS)
  # =========================================================================
  desktop-smoke-macos:
    name: Desktop Smoke (macOS)
    runs-on: macos-latest
    timeout-minutes: 20
    needs: [go-cross-build, ts-clean-build]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Build agent (macOS)
        working-directory: runtime-agent
        run: go build -trimpath -o ../dist/kairo-runtime ./cmd/kairo-runtime

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Start agent (macOS)
        run: |
          ./dist/kairo-runtime --bind 127.0.0.1 --port 18080 &
          echo "AGENT_PID=$!" >> $GITHUB_ENV
          sleep 3

      - name: Smoke test (health check)
        run: |
          for i in $(seq 1 10); do
            if curl -fsS http://127.0.0.1:18080/api/v1/health | grep -q '"ok":true'; then
              echo "Agent healthy"
              exit 0
            fi
            sleep 1
          done
          echo "Agent not healthy within 10s" >&2
          exit 1

      - name: Stop agent
        if: always()
        run: |
          if [ -n "$AGENT_PID" ]; then
            kill $AGENT_PID 2>/dev/null || true
          fi

      - name: Upload agent logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: macos-agent-logs
          path: /tmp/kairo-agent*.log
          if-no-files-found: ignore

  # =========================================================================
  # Release gate — all required jobs must pass
  # =========================================================================
  release-gate:
    name: Release Gate
    runs-on: ubuntu-latest
    needs:
      - go-unit-linux
      - go-cross-build
      - ts-clean-build
      - contract
      - integration-core
    if: always()
    steps:
      - name: Check all required jobs
        run: |
          echo "Checking required jobs..."
          FAILED=0

          check_job() {
            local result="$1"
            if [ "$result" != "success" ]; then
              echo "FAILED: $2"
              FAILED=1
            else
              echo "PASSED: $2"
            fi
          }

          check_job "${{ needs.go-unit-linux.result }}" "go-unit-linux"
          check_job "${{ needs.go-cross-build.result }}" "go-cross-build"
          check_job "${{ needs.ts-clean-build.result }}" "ts-clean-build"
          check_job "${{ needs.contract.result }}" "contract"
          check_job "${{ needs.integration-core.result }}" "integration-core"

          if [ "$FAILED" -eq 1 ]; then
            echo "Release gate FAILED"
            exit 1
          fi
          echo "Release gate PASSED"