# 第6章 工具栏测试结果（Browser 列）

- 通道: lane A（THEIA_URL=http://127.0.0.1:18401, AGENT_PORT=18400）
- 测试文件: `tests/e2e/comprehensive/ch06-toolbar.spec.ts`
- 最终运行: 2026-08-26，**11 用例：11 通过 / 0 跳过 / 0 失败**

## 结果表

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-TB-001 | ✅ | `TC-TB-001 toolbar shows all elements` | Project 下拉、Run Config 下拉、竖分隔线、Run▶/Debug/Stop/Build 四按钮 + codicon 图标齐全（依赖 BUG-100 修复） |
| TC-TB-002 | ✅ | `TC-TB-002 project dropdown lists agent projects and marks current` | 列出 legacy-sample/_proj-second；无项目时 "No projects"（ch05 空态亦验证） |
| TC-TB-003 | ✅ | `TC-TB-003 switching project updates ActiveProjectService (status bar follows)` | 切换 _proj-second ↔ legacy-sample，状态栏 Project 条目同步变化 |
| TC-TB-004 | ✅ | `TC-TB-004 run config dropdown format "{name} ({MODE})"` | "Tomcat Local (RUN)"、"Tomcat Debug (DEBUG)" 格式正确；空态 "No configurations" 在初始页验证 |
| TC-TB-005 | ✅ | `TC-TB-005 selecting a config persists selectedConfigurationId` | 选择另一配置后 GET run-configurations 的 selectedConfigurationId 同步（不足两条配置时条件跳过，本轮已满足） |
| TC-TB-006 | ✅ | `TC-TB-006 Run button enters busy state and restores (launch request intercepted)` | 拦截 POST /api/v1/servers：busy 根类 kairo-toolbar-busy、"Running…"文案、四按钮全禁，完成后恢复 |
| TC-TB-007 | ✅ | `TC-TB-007 Debug button disabled when selected config mode=run` | mode=run 配置下 Debug 禁用 |
| TC-TB-008 | ✅ | `TC-TB-008 Stop button gating` | mock running server → Stop 启用；点击变 "Stopping…" 后恢复；空闲时禁用 |
| TC-TB-009 | ✅ | `TC-TB-009 Build button shows Building… and creates a Builds record` | "Building…" 文案 + agent builds 历史新增记录 |
| TC-TB-010 | ✅ | `TC-TB-010 double-click Run triggers exactly one operation` | 双击仅 1 次 POST /servers（busy 单操作锁） |
| TC-TB-011 | ✅ | `TC-TB-011 toolbar labels refresh on language switch to zh-CN` | 写入 theia-config settings.json 的 kairo.language=zh-CN，工具栏 Build 标签实时切换中文 |

## 缺陷表

| 缺陷号 | 文件:行号 | 根因 | 修法 | 状态 |
|--------|-----------|------|------|------|
| （本章无独立缺陷；依赖 ch05 的 BUG-20260826-100 工具栏挂载修复后才能执行） | — | — | — | — |

## 说明
- Run/Stop/Build 忙态与防重复通过 Playwright route 拦截对应 agent 端点验证，无需真实 Tomcat。
- i18n 切换直接编辑 `THEIA_CONFIG_DIR/settings.json`（Theia 热加载偏好），等价用户在设置中切换语言。
