# 第7章 状态栏测试结果（Browser 列）

- 通道: lane A（THEIA_URL=http://127.0.0.1:18401, AGENT_PORT=18400）
- 测试文件: `tests/e2e/comprehensive/ch07-statusbar.spec.ts`
- 最终运行: 2026-08-26，**11 用例：11 通过 / 0 跳过 / 0 失败**

## 结果表

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SB-001 | ✅ | `TC-SB-001 Project entry…` | 无项目时灰显 placeholder；点击打开项目选择器；激活后显示 legacy-sample |
| TC-SB-002 | ✅ | `TC-SB-002 JDK entry…` | 显示 JDK major/state（本通道 OpenJDK21 自动探测前为 stopped），含语义状态 class |
| TC-SB-003 | ✅ | `TC-SB-003 Encoding entry…` | 无编辑器 placeholder；打开文件显示当前编码并点击弹出 Reopen with Encoding QuickPick。注：GBK 显示需激活已导入项目（编码注册表按项目生效），utf-8 全局默认亦满足“显示当前编码”；GBK 专项在 ch17 |
| TC-SB-004 | ✅ | `TC-SB-004 Build entry reflects build states` | 触发构建期间/结束观察到 kairo-statusbar-state-active/success 语义类，终态 success 绿 |
| TC-SB-005 | ✅ | `TC-SB-005 Server entry shows state with port info` | mock running server 后显示 "Server: running :18080 · JDWP:8000"，active/success 着色；空闲态 stopped neutral 在默认加载验证 |
| TC-SB-006 | ✅ | `TC-SB-006 Debug entry present and neutral while idle` | Debug 条目存在且属 group-3（neutral） |
| TC-SB-007 | ✅ | `TC-SB-007 Agent entry connected; click reconnect keeps connection` | "Agent: connected" + success 色；点击 reconnect 后仍 connected |
| TC-SB-008 | ✅ | `TC-SB-008 Hot Reload entry only visible with an active project` | 项目激活后出现 HotReload 条目（synced，success 类）；未激活项目时不渲染 |
| TC-SB-011 | ✅ | `TC-SB-011 Large file indicator appears for >5M char files` | 6MB 文件打开后出现 "$(dashboard) Large file mode"（修复 BUG-20260826-111 后）；>50M Huge 档因体积原因未测 |
| TC-SB-012 | ✅ | `TC-SB-012 Format on Save indicator mirrors preference on java files` | 打开 .java 后右侧出现 Format on Save 指示，codicon-close/check 反映开关 |
| TC-SB-013 | ✅ | `TC-SB-013 Notification bell shows unread badge and opens notification center` | 铃铛含未读徽标数字；点击打开通知中心 |

## 缺陷表

| 缺陷号 | 文件:行号 | 根因 | 修法 | 状态 |
|--------|-----------|------|------|------|
| BUG-20260826-111 | packages/theia-product/src/main/browser/kairo-large-file-contribution.ts:78 | onCreated 回调时大文档 model 未加载（getValueLength()=0）→ 初始分级恒 normal，Large/Huge 指示永不出现；且 model 未挂载时 manage() 直接 return 永不重试 | attachWhenReady 轮询等待 model 挂载；内容首次到达后再分级 | 已修复并回归 |

## 说明
- Server 条目的 running 态通过拦截 GET /api/v1/servers 并阻断 events WS 注入 mock 数据验证（避免真实 crashed 记录干扰）。
- 状态栏条目 DOM 为 `#theia-statusBar .element`（kairo-statusbar-group-N / kairo-statusbar-state-* 语义类），非 Theia 默认 `.lm-StatusBar-item`。
