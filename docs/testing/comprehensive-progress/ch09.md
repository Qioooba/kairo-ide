# 第9章 项目导入向导测试结果 — Browser 列

> 执行日期: 2026-08-27 · 通道 B (THEIA_URL=http://127.0.0.1:18411, AGENT_PORT=18410)
> Spec: `tests/e2e/comprehensive/ch09-import.spec.ts`（serial，18 个 test 覆盖 17 条用例，007 为 Windows N/A）
> 最终回归: **18 passed (6.2m)** — 全部通过

## 用例结果

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-IMP-001 | ✅ | 向导 tab 打开，`step-1` aria-current=step，`welcome-tips` 可见且 `li` 4 条，`path-input` 可见 | 无 |
| TC-IMP-002 | ✅ | 点 `browse-btn` 弹出 `.theia-FileDialog`，选行后自动扫描进 `step-content-2`，`input-project-name` 含值 | 无 |
| TC-IMP-003 | ✅ | `path-input` 填入路径 + Enter 触发 `detectProject`，`step-content-2` 出现且 `input-project-name` 为目录名，`select-build-tool` 为 ant | 无 |
| TC-IMP-004 | ✅ | 空路径时 `scan-btn` disabled；Enter 空输入显示 `role=alert`/`scan-error` 含 /输入路径/，`trackDetect()` 计数 0，未发 detect 请求，停留 Step1 | 无 |
| TC-IMP-005 | ✅ | 不存在子路径 Scan 后 `scan-error` role=alert 文案匹配 /no such file\|不存在/，停留 Step1，无 Step2 | 无 |
| TC-IMP-006 | ✅ | 越权路径 `/Users/qi/.ssh` Scan 后 `scan-error` 文案匹配 /forbidden\|越权/，停留 Step1 | 无 |
| TC-IMP-007 | ⏭️ | Windows 反斜杠路径 `C:\test\data` → N/A（macOS） | — |
| TC-IMP-008 | ✅ | 中文空格路径 `测试 项目/cn-proj` 检测成功，Step2 name=cn-proj，完成导入后 `ready-root` 含 `测试 项目/cn-proj` 无乱码 | 无 |
| TC-IMP-011 | ✅ | legacy-sample 自动填充：name, `src/main/java`, WebRoot, lib, build.xml, ant, gbk, 1.6, build/classes, / | 无 |
| TC-IMP-012 | ✅ | 顶部 `.kairo-confidence` 含 85%（Math.round(confidence*100)），`detection-warnings` 当 bare 目录时可见 | 无 |
| TC-IMP-013 | ✅ | 无提示目录回退为 gbk；`bundle.js` 含 `case "utf8": return "utf-8"` 归一化验证 | 无 |
| TC-IMP-014 | ✅ | 清空 `input-project-name` 点导入，`import-error` 可见含 /name\|必填/，import 计数 0，停留 Step2 | 无 |
| TC-IMP-015 | ✅ | sourceDirs `src, test ,` 提交 payload `sourceDirs: ["src","test"]`（trim+filter） | 无 |
| TC-IMP-016 | ✅ | 点 `back-to-select` 回 Step1，`path-input` 保留原值，重新 Enter 可再进 Step2 | 无 |
| TC-IMP-021 | ✅ | 正常导入顺序：`openWorkspace` → `projects/import`（严格顺序），Step3 出现，状态栏 project 含 imp-021 | 无 |
| TC-IMP-022 | ✅ | 停 agent 后导入，`import-error` role=alert 含 /error\|错误/，停 Step2，工作区未产生孤儿（仅 1 authorized 绑定） | 无 |
| TC-IMP-023 | ✅ | 同 root 二次导入：先 API 导入一次，再向导重导入，冲突后匹配既有绑定，正常进 Step3，`ready-name` 为 imp-023 | **测试基建修正后通过** |
| TC-IMP-031 | ✅ | 完成页三行摘要：`ready-name`=imp-031，`ready-root`=realpath( dir )，`ready-encoding`=gbk | 无 |
| TC-IMP-032 | ✅ | 点 `open-project-btn` 以 file:// 打开 workspace，hash 含 imp-032，文件树含 build.xml，Welcome 关闭 | 无 |
| TC-IMP-033 | ✅ | 点 `ready-close-btn` 关闭向导 tab，无副作用（workspace/project 状态保留） | 同 031 合测 |
| TC-IMP-034 | ⏭️ | open 失败兜底 console.error 且 onClose — 仅 DESKTOP（文档标记 B=—），Browser N/A | — |

## 测试基建修正（非产品缺陷）

- `openWizard()` 原等待 `#theia-main-content-panel .lm-TabBar-tab:Welcome|Import` 超时于 TC-IMP-023：该用例先通过 API 已导入项目，`openIdeAt(dir)` 后 Welcome 自动关闭（有活跃项目），无 Welcome/Import tab。修法：`tests/e2e/comprehensive/ch09-import.spec.ts:21` 移除 tab 等待，改用 shell 沉降 + CTA 可见性探测，CTA 不可见则走 `runCommand('Import Project')`。

---
*执行人: W1-B agent · 通道 B 6.2m 回归通过*
