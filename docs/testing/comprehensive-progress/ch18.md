# 第18章 搜索功能全家桶 · Browser 列测试报告（Wave2）

> **通道**: C lane（THEIA_URL=http://127.0.0.1:18421，AGENT_PORT=18420，无鉴权；W2 fixture 绑定 `.test-lanes/A/workspace`）
> **工作区**: 只读用例绑定 lane workspace（legacy-sample）；Replace/排除类用例使用独立的 W2 目录 `.test-lanes/A/workspace/kairo-w2search/ws`（经 URL fragment 绑定 + agent workspace 注册），**未污染共享工作区**。
> **平台说明**: 本轮为 Windows Chromium，Kairo 键位走平台映射：Ctrl+Shift+F / Ctrl+Shift+R / 双击 Shift / Ctrl+Shift+N / Ctrl+N / Ctrl+Alt+Shift+N / Ctrl+Shift+A。浏览器守卫与 Theia 注册表均按 Windows 处理，避免 Meta 键在 Windows 上被误发或被 Monaco 默认绑定抢占。
> **测试代码**: `tests/e2e/comprehensive/ch18-search.spec.ts`（18.1+18.2）、`ch18-search-b.spec.ts`（18.3+18.4）、`ch18-search-c.spec.ts`（18.5）、公共 `ch18-helpers.ts`
> **结果**: **27 个自动化用例全绿**（Windows Chromium 串行：17 + 8 + 2；修复底部 Find 窗流式数组缓存失效后重跑通过），覆盖 B 列全部 27 条 TC。

---

## 一、用例结果（B=Browser 列）

### 18.1 Search Center

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SRCH-001 | ✅ | Windows Chromium 中 `Ctrl+Shift+F` 打开 `[data-testid=search-center-modal]`，backdrop 在 `#theia-app-shell` 之外、widget 根直挂 `document.body`；SIW 面板 rect=0×0 未被拉起；输入框自动聚焦；Esc 关闭 | 平台键位由 helper 统一映射（mac=Cmd，Windows/Linux=Ctrl） |
| TC-SRCH-002 | ✅ | 先经 Go→Go to File 打开 HelloWorld.java 并聚焦 Monaco，Windows `Ctrl+Shift+F` 仍弹出；modal 内输入 "Hello"+x → 值为 `Hellox`（放行）；Esc 仅关一次 | capture 监听 + guard 对 modal 内 target 直通 |
| TC-SRCH-003 | ✅ | 三按钮 is-active 态正确；专用 flagsample.txt 上行为断言：Aa on `BetaToken`=1 条 / off `BETATOKEN`=1 条；W on `tok`=1 条(word-bounded) / off=3 条；.* on `T.KEN`=3 条(含 Token) / off=1 条(literal)。init-script 包裹 WebSocket 捕获 framesent：各请求携带 caseSensitive/isRegex/wholeWord/include:['flagsample.txt'] 与开关一致 | Playwright 页面级 ws 事件漏采该 socket → 改用 addInitScript 包装捕获 |
| TC-SRCH-004 | ✅ | W2 搜 'e'：WS framesent/received 显示 batchIndex 0..N 严格递增、每批 ≤50、total 单调增长（>50 条 ⇒ ≥2 批，实测上千批至 100000 截断），随后 done 帧；DOM `[data-testid=search-count]` 流式期间显示 "(streaming)" 且计数逐步增长（采样序列递增）；终态统计 = totalMatches in N files，无 error | 60s idle 看门狗逻辑在源码 search-stream-service.ts:32,91-109，本轮未触发（流正常收尾） |
| TC-SRCH-005 | ✅ | 'foo' 命中 alpha.txt×4 + beta.txt×2：2 个 `[data-testid=search-group]` 头（文件名+计数徽章）；← 折叠选中行所属组（行数 6→4，chevron-right 出现）→ 连续 ← 全折叠（0 行/2 chevron）→ → 展开（行数恢复>0） | BUG-500 修复后 ←/→ 才可用 |
| TC-SRCH-006 | ✅ | 选择自动落在首个匹配行；↓ 逐匹配推进且跨组不耗按键（effectiveDown == rows-1-i0，每次恰好 +1 行）；↑ 回到起点；Enter 开编辑器且弹层留存（tab 数增加）；Shift+Enter 开并关弹层；Ctrl+Enter reveal 编辑器（tab 增加）且弹层保持挂载 | pressInModal 每 keypress 重锚焦点（Theia 偶发吞键/夺焦，见备注⑤） |
| TC-SRCH-007 | ✅ | Open in Find Window → 底部面板 `[data-testid=search-results-panel]` 可见且 `find-tool-count` = "N matches in M files"；重开弹层结果仍在（共享 session model）；Esc 关弹层后 docked 行依旧可见 | 流式结果数组复用时以 `revision` 触发 docked 分组重算，避免统计更新但行列表为空 |
| TC-SRCH-008 | ✅ | 无掩码搜 'class'：readme.txt + Util/Greeter/TestHolder.java 均命中；掩码 `java, !Test` 后仅剩 *.java 且无 TestHolder.java（include 收窄排除 readme.txt、!Test 排除 Test 文件）| BUG-503 修复后 !Test 才生效 |
| TC-SRCH-009 | ✅ | Current File → include=[Greeter.java 相对路径]，结果唯一组 Greeter.java；Project 恢复全集；Directory 结果路径全部以 com/example 结尾 | selectOption 驱动 resolveScope()（search-center-widget.tsx:879+） |
| TC-SRCH-010 | ✅ | toggle-advanced 展开 exclude 输入；exclude `**/*.java` 后 .java 组全部消失、readme.txt 保留 | |
| TC-SRCH-011 | ✅ | 3 次搜索（zebra/token/zebra 重复）后 localStorage `kairo-search-history` 长度=2 且去重最新在前；历史菜单可见，zebra 可一键固定并写入 `kairo-search-pinned`；预置 55 条再搜一次 → ≤50 且首位为新查询 | history 菜单同时支持恢复、固定/取消固定、单项移除和清空；模型仍强制 history 50 / pinned 10 上限 |
| TC-SRCH-012 | ✅ | 'e' 流式中 Cancel → `[data-testid=search-cancelled]` 出现且**无** error 横幅（CancelledError 不当错误展示 ✓）；随即 'greet' 正常出结果（仅 Greeter.java）；/search/stream 连接计数 ≥2（旧流 abort、新流独立）| |

### 18.2 Replace in Path（隔离目录 `.test-lanes/A/workspace/kairo-w2search/ws/replace`）

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SRCH-021 | ✅ | Windows `Ctrl+Shift+R` 进 Replace tab；'foo' 4+ 条结果；填入替换文本即出现 Replace all 按钮（plan 即时生成）；结果行内渲染后像列 `.kairo-search-replacement`=bar（match→replacement 预览）| BUG-501 修复项 |
| TC-SRCH-022 | ✅ | 两文件事务落盘：alpha/beta 字节级断言 foo→bar 全部替换；applied 且无 failed ⇒ Undo 按钮出现 | preflight(FNV-1a)+条件写路径 search-replace-service.ts:38-96 |
| TC-SRCH-023 | ✅ | 应用 REPL 后 Undo → 按钮消失，alpha/beta 恢复为原始字节（先验 post-image、逆序回写 original 的对外表现）| 中途失败的补偿分支由单测覆盖（94 unit tests） |
| TC-SRCH-024 | ✅ | huge.txt 5.66MB(>5MB, zebra×2)：搜索出 2 条但填入替换词后 **Replace all 按钮不出现**（createPlan 触发 5MB read-limit 异常被吞），huge.txt 字节不变，无崩溃 | \0 二进制文件在搜索引擎层已被扩展名/编码嗅探跳过（Fake*.class 从不入结果，见 052），故端到端仅超大文件可达 replace 层 |
| TC-SRCH-025 | ✅ | plan 生成后外部 append beta.txt → Replace All → alpha 保持原样、beta 仅多手工行（**零写入**），Undo 不出现（canUndo=false），并显示错误横幅 | 错误横幅为 BUG-502 修复项 |

### 18.3 Search Everywhere

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SRCH-031 | ✅ | 双击 Shift 间隔 70ms → body overlay `[data-testid=search-everywhere]`（壳外宿主）出现且输入框聚焦；间隔 650ms 不触发；输入框内双 Shift 被忽略（值保持 'Util'）；Esc 关闭 | Esc 关闭为 BUG-504 修复项 |
| TC-SRCH-032 | ✅ | files/types/symbols/actions/all 各 tab 均落位 results 或优雅空态，overlay 内 `[role=alert]` 恒为 0；files 搜 HelloWorld 命中、actions 搜 Save 命中、all 混合正常 | 并发 allSettled 部分失败降级：providers 单个 reject 不致 error（search-everywhere-model.ts:88-93），JDT 停止时 symbols 提供者 reject 即为此路径的实证 |
| TC-SRCH-033 | ✅ | 查询 "HeloWor"（非子串）types/files 首条均命中 HelloWorld(.java)，top1 断言通过 | fuzzyScore 子序列+词边界加权见 search-everywhere-model.ts:37-55 |

### 18.4 Find File / Class / Symbol / Action

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SRCH-041 | ✅ | Windows `Ctrl+Shift+N` 弹 find-file；查 'Util' 首条 label=Util.java、detail=src/Util.java（agent file-index maxFiles 50000/TTL 15s 路径，file-index-service.ts）；Enter 开编辑器并关弹层；重开空查询 → recent ≥1 且首条 Util.java（remember 上限 20）；agent 不可用时回退 BFS≤10000（find-file-model.ts:159-191，代码路径） | macOS 使用对应 Cmd 映射 |
| TC-SRCH-042 | ✅ | Windows `Ctrl+N` 弹 find-class；查 'Greeter' → 首条 Greeter，detail=com.example（包名剥离启发式 +40 分支，find-class-model.ts:182-238）；Enter 打开 Greeter.java；3s 缓存且不缓存空结果（:133-137,170-174） | macOS 使用对应 Cmd 映射；JDT ready 时叠加 workspaceSymbols(+50 source bonus) 分支在 :76-89 |
| TC-SRCH-043 | ✅(降级) | 本通道 JDT 无法启动（ADR-0014：LS 进程生命周期归 Theia backend，agent 仅 Prepare 分发；多次 prepare + 打开 .java 编辑器轮询 2min+ state 恒 stopped）。修复后的行为：workspaceSymbols reject → 降级 "No results"，overlay 内无错误横幅。kinds 过滤集合 {Method,Property,Field,Constructor,Function,Variable,Constant} 于 find-symbol-widget.tsx SYMBOL_KINDS | 正向符号路径需 JDT 就绪环境复测（遗留①） |
| TC-SRCH-044 | ✅ | 查 'Save' → 27 条带 label 命令，首条含 "Save" 且 detail="Cmd + s"（格式化快捷键）；排序按 fuzzy 分数；无 _ 开头/不可见命令混入 | BUG-506 修复后排名才正确 |
| TC-SRCH-045 | ✅ | 直接对随 bundle 编译产物 `packages/search-extension/lib/browser/search-path.js` 断言：../、../../、绝对路径、file:///、file://localhost、%2e%2e（decode 后）、a/../../b 全部抛 Unsafe/escapes；合法相对路径解析到 workspaceRoot 之下 | resolveWorkspaceMatchUri 同时在 UI 打开匹配时调用 |

### 18.5 docked Find

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-SRCH-051 | ✅ | F1 '>Open Find Tool Window'（kairo.search.results.open）→ 底部 rank150 区域出现 Find 窗（idle 占位）；弹层发起搜索后 docked 实时镜像同计数同行数；关闭弹层后 docked 结果保留 | 共享 KairoSearchSessionModel（DI 单例） |
| TC-SRCH-052 | ✅ | zebraunique 同时埋在根目录 txt、node_modules/pkg/deep.js、.git/hidden.txt，WEB-INF/lib 下另有 Fake2.class：搜索仅返回 deepsignal.txt 1 条 —— node_modules/.git 目录级默认排除 + *.class 二进制扩展跳过共同生效（runtime-agent/internal/search/search.go DefaultExcludes/isLikelyBinary） | 注：WEB-INF/lib 排除经由 *.class/*.jar 二进制跳过实现（DefaultExcludes 无该目录名） |

---

## 二、缺陷台账（本次修复 7 项，编号自 500 起）

| 缺陷号 | 等级 | 文件:位置 | 问题 | 修法 |
|--------|------|-----------|------|------|
| BUG-20260826-500 | P2 | packages/search-extension/src/browser/search-center-widget.tsx（选择重置 useEffect） | flatItems 变化即把选择重置回首个匹配行：← 折叠后选择永远落在匹配行上，→ 无法到达折叠头，**折叠后键盘不可展开**（TC-005 死锁） | navKey=`requestId:matches.length` 守卫，仅结果集变化时重置选择；折叠/展开不再打断导航 |
| BUG-20260826-501 | P2 | search-center-widget.tsx submit()+rows、search-result-utils.ts、search-center.css | Replace 模式无 replacement 预览：请求不带 previewReplace、行渲染无后像列（协议字段闲置） | replace 模式提交附带 previewReplace；行内从 replacePlan 就地解析 after 渲染 `match→replacement` 绿色预览（docked 窗沿用 util 的 replacement 渲染） |
| BUG-20260826-502 | P2 | search-center-widget.tsx apply onClick | Replace All 失败（preflight 漂移/写失败/rollback 失败）完全静默：applyResults 存了但不渲染，用户无法区分成功与零写入失败 | results 含 failed/rollback-* → setSubmissionError 展示错误横幅；成功时清除 |
| BUG-20260826-503 | P2 | file-mask.ts parseFileMask | `!Test` 裸词排除被展开成伪扩展名 `*.Test`，真实文件名永不命中，**排除形同虚设**（TC-008 TestHolder.java 漏排） | 裸词排除改为文件名子串 `*Test*`；补单测两条（file-mask.test.cjs） |
| BUG-20260826-504 | P2 | search-everywhere-widget.tsx、find-file/class/symbol/action-widget.tsx ×5 | Search Everywhere/Find File·Class·Symbol·Action 弹层 **Escape 无法关闭**（组件留有 "handled by widget close" 空注释，无人实现） | Everywhere 组件补 onClose 分支；五个 widget 统一加 window capture Escape 监听（onAfterAttach 注册 / onBeforeDetach 注销） |
| BUG-20260826-505 | P2 | search-center-widget.tsx keyDown | 方向键 preventDefault 但未 stopPropagation，事件继续冒泡被 Theia 捕获，**焦点被夺到 Navigator 文件树**，弹层键盘导航随机中断（probe9 实证 activeElement 变 theia-FileTree） | 所有已处理导航键补 stopPropagation() |
| BUG-20260826-506 | P2 | find-action-model.ts query | `fuzzyScore(...) ?? 0` 使后续 score!==undefined 过滤永远为真：不匹配命令全量入选且按字典序排列（搜 "Toggle Sidebar" 首条是 About，TC-044 失败根因） | 去掉 `?? 0`，恢复 undefined 过滤 + 评分排序 |

**未修（遗留）**

| 编号 | 等级 | 说明 |
|------|------|------|
| 遗留① | 环境 | JDT LS 在浏览器 lane 无法启动（agent prepare 只装分发；进程归 Theia backend 管理）。TC-SRCH-043 正向符号路径（workspaceSymbols kinds 过滤）需在具备 JDT 就绪的环境补测；本通道验证了停机降级路径。 |

---

## 三、其他备注

1. **Playwright 页面级 websocket 事件采不到 `/search/stream`**（socket.io 正常）→ 用 `addInitScript` 包装 `window.WebSocket` 捕获 sent/received/连接数，证据更直接（ch18-search.spec.ts `installStreamCapture`）。
2. Theia overlay 在导航键渲染后偶发吞键/焦点漂移（probe9 未发现显式 focus() 调用方）→ 测试侧 `pressInModal` 每次 keypress 重锚 `[data-testid=search-count]` 焦点并对 swallowed press 重试；产品侧已做 stopPropagation 止血（BUG-505）。
3. Replace 类用例全部在 `.test-lanes/A/workspace/kairo-w2search/ws` 进行（独立 agent workspace + URL fragment 绑定），共享 lane workspace 只读未污染；flagsample.txt/deepsignal.txt 等探针文件亦在该目录。
4. 回归状态：ch18 三个 spec **27/27 通过**（Windows Chromium 串行）；smoke.spec 3/3 通过；@kairo/search-extension 单测 **96/96 通过**。
