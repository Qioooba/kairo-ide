# 第37章 命令面板与焦点导航 — Browser 列测试结果

> 通道 C | THEIA_URL=http://127.0.0.1:18421 | AGENT_PORT=18420
> Spec: `tests/e2e/comprehensive/ch37-cmdpalette.spec.ts`
> 运行日期: 2026-08-26　最终结果: **5 通过 / 0 失败 / 0 阻断 / 0 N/A**

| 编号 | 结果 | 证据 | 缺陷/备注 |
|------|------|------|-----------|
| TC-CMD-001 命令面板 | ✅通过 | F1 打开 quick-input（'>' 前缀）；输入 `kairo.` 列出 17+ 条命令（虚拟列表可视区计数，全量注册 83 个 kairo.* id）；执行无害命令 `Focus Editor` 后焦点落到主区、面板关闭、无 pageerror | 标签未发现 raw i18n key 泄漏；i18n 中英文切换的全量验证归第42章。已知遗留 BUG-112（Go To 子菜单缺 Java 项）属菜单层，命令面板中 `kairo.java.*` 可搜到，对本章断言无影响 |
| TC-CMD-002 Find Action | ✅通过 | **浏览器实际键位 = ⌘⇧A**（附录B.3 仅 remap Chrome 保留键；macOS 遵循 B.2 ⌘⇧A，已记录）；`.kairo-find-modal` 打开，查询 "close" 过滤出 27 项，全部含 label，shortcut detail 列显示 "View"/"Workspaces"/"alt + w" 等；Escape 关闭浮层 | **修复 BUG-20260826-309**：浮层原本不聚焦查询框（autoFocus 被 overlay 挂载时序击败），焦点留在树组件上导致 Escape 无效 |
| TC-CMD-003 Escape 焦点返回 | ✅通过 | 焦点在 Explorer 树（left）按 Esc → activeElement 落入主编辑器区（main） | **修复（随 BUG-20260826-310 记录）**：原 bubble 级监听被文件树 target 层 Escape（取消 type-ahead）截胡；改为 document capture 监听，豁免 INPUT/TEXTAREA/contenteditable/.monaco-editor/#theia-dialog-shell/[role=dialog] 目标，保住 BUG-303 的对话框契约 |
| TC-CMD-004 F6 面板轮巡 | ✅通过 | 从编辑器连按 F6：left → … → statusbar → main 循环（PANEL_ORDER editor→sidebar→bottom→statusBar），覆盖 ≥3 个不同区域并回绕 | bottom 隐藏时 focusBottomPanel 不 reveal（仅聚焦可见元素），轮巡中该站可能跳过——与 IDEA 行为一致，不算缺陷 |
| TC-CMD-005 Hide panel | ✅通过 | 经 `View: Toggle Output` 显示底栏 → Shift+Escape → 底栏 display:none 隐藏 ✓ | kairo.hideActivePanel 为 BUG-303 修复中新增的真实命令；F1 偶发未弹出的启动竞态用重试逻辑压制 |

## 本章修复的缺陷

### BUG-20260826-309 — Find Action 浮层不聚焦查询框（P2）
见 ch12.md 同名条目。位置 `packages/search-extension/src/browser/find-action-widget.tsx`。

### 关联修复 — Escape→Editor 升级为 capture 监听
见 ch12.md BUG-20260826-310 附带第 2 点；同时保留 `[role="dialog"]` 豁免，Find Action 自身的 Escape 关闭不受影响。

## 备注
- CMD-002 键位记录：桌面 Win Ctrl+Shift+A → macOS ⌘⇧A → 浏览器版无 remap（附录B.3 只覆盖 Ctrl+N/W 类 Chrome 保留键），实际执行键即 Meta+Shift+A。
- F1 偶发首按不弹（workbench 启动竞态），spec 以 3 次重试兜底；产品层可在后续版本排查 quick-input 首开时序。
