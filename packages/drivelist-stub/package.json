{
  "name": "drivelist",
  "version": "12.0.2",
  "description": "Kairo pure-JS drivelist — lists Windows A–Z volumes and Unix root without native bindings.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node ../../scripts/clean-dir.cjs lib",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --require ts-node/register --require source-map-support/register --test src/list.test.ts src/list-extended.test.ts src/list-edge-cases.test.ts"
  }
}
