{
  "name": "@kairo/encoding-extension",
  "version": "0.1.0",
  "description": "Kairo encoding integration — bridges Monaco / Theia to the Runtime Agent's encoding endpoints so GBK / GB18030 / UTF-8-BOM / ISO-8859-1 files round-trip safely.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --require ts-node/register --require source-map-support/register --test src/browser/encoding-service.test.ts src/browser/encoding-reopen.test.ts && pnpm build && node --import ./test/register-hooks.mjs --test src/browser/encoding-uri-coercion.test.cjs"
  },
  "dependencies": {
    "@kairo/project-extension": "workspace:*",
    "@kairo/protocol": "workspace:*",
    "@kairo/runtime-extension": "workspace:*",
    "@theia/core": "1.73.1",
    "@theia/filesystem": "1.73.1"
  },
  "devDependencies": {
    "iconv-lite": "^0.7.3",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "7.0.2"
  }
}
