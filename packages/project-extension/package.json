{
  "name": "@kairo/project-extension",
  "version": "0.1.0",
  "description": "Kairo project extension �?owns the workspace and project models, the welcome view, and the project import wizard.",
  "main": "lib/index.js",
  "types": "lib/src/index.d.ts",
  "files": ["lib", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -r ./test-setup.cjs -r ts-node/register --no-warnings src/browser/import-wizard-widget.test.ts"
  },
  "dependencies": {
    "@kairo/protocol": "workspace:*",
    "@kairo/runtime-extension": "workspace:*",
    "@kairo/config-schema": "workspace:*",
    "@kairo/ui-kit": "workspace:*"
  },
  "peerDependencies": {
    "@theia/core": "1.73.1",
    "react": "18.3.1"
  },
  "devDependencies": {
    "@types/react": "18.3.1",
    "typescript": "5.5.4"
  }
}

