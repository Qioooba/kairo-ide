{
  "name": "@kairo/protocol",
  "version": "0.1.0",
  "description": "Kairo wire protocol: shapes, error codes, and versioned endpoint definitions shared by Theia extensions and the Go Runtime Agent.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --require ts-node/register --require source-map-support/register --test src/network.test.ts src/envelope.test.ts src/protocol-extended.test.ts"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "20.14.10"
  }
}

