{
  "name": "@kairo/protocol",
  "version": "0.1.0",
  "description": "Kairo wire protocol: shapes, error codes, and versioned endpoint definitions shared by Theia extensions and the Go Runtime Agent.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --require ts-node/register --require source-map-support/register --test src/network.test.ts src/envelope.test.ts src/protocol-extended.test.ts src/network-edge-cases.test.ts src/envelope-edge-cases.test.ts src/endpoint-map-routes.test.cjs"
  },
  "devDependencies": {
    "typescript": "5.5.4",
    "@types/node": "26.4.0"
  }
}

